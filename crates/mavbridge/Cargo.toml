[package]
name = "mavbridge"
version = "0.1.0"
edition = "2024"

[dependencies]
mavlink = { version = "0.18.0", default-features = false, features = ["dialect-common", "embedded", "mav2-message-extensions"] }
